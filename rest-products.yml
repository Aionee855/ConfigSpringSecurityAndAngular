spring:
  application:
    name: rest-products

server:
  port: 9081
  
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8080/eureka/
      
security:
  basic:
    enabled: false
  oauth2:
    resource:
      userInfoUri: http://localhost:8000/user

custom-var:
  role: ROLE_ADMIN
